import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.florium',
  appName: 'Florium',
  webDir: 'dist/florium-frontend/browser'
};

export default config;
